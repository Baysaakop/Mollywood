import React, { useState } from 'react';
import { Paper } from '@material-ui/core';

const Artists = (props) => {
    return (
        <Paper>
            <h1>Artists</h1>
        </Paper>
    );
};

export default Artists;