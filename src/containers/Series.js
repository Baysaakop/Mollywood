import React, { useState } from 'react';
import { Paper } from '@material-ui/core';
import Breadcrumb from '../components/Breadcrumb';

const Series = (props) => {
    return (
        <Breadcrumb page="ЦУВРАЛ ЖАГСААЛТ" />
    );
};

export default Series;